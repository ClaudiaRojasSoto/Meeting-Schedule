<div class="mb-8">
  <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
    <div>
      <div class="flex items-center gap-3 mb-2">
        <span class="text-5xl">👥</span>
        <h1 class="text-4xl font-bold bg-gradient-to-r from-purple-800 to-rose-700 bg-clip-text text-transparent">
          Gestión de Usuarios
        </h1>
      </div>
      <p class="text-gray-600 text-lg">Administra los usuarios del sistema</p>
    </div>
    <%= link_to new_admin_user_path, class: "inline-flex items-center justify-center gap-2 px-6 py-3 bg-gradient-to-r from-amber-600 to-amber-700 text-white rounded-xl font-semibold shadow-lg hover:shadow-xl transform hover:scale-105 transition duration-200" do %>
      <span class="text-xl">➕</span>
      Agregar Usuario
    <% end %>
  </div>
</div>

<div class="bg-white shadow-xl rounded-2xl overflow-hidden border border-gray-100">
  <div class="overflow-x-auto">
    <table class="min-w-full divide-y divide-gray-200">
      <thead class="bg-gradient-to-r from-purple-100 to-rose-100">
        <tr>
          <th class="px-6 py-4 text-left text-xs font-bold text-purple-900 uppercase tracking-wider">
            <div class="flex items-center gap-2">
              <span>📧</span>
              Email
            </div>
          </th>
          <th class="px-6 py-4 text-left text-xs font-bold text-purple-900 uppercase tracking-wider">
            <div class="flex items-center gap-2">
              <span>👤</span>
              Nombre
            </div>
          </th>
          <th class="px-6 py-4 text-left text-xs font-bold text-purple-900 uppercase tracking-wider">
            <div class="flex items-center gap-2">
              <span>🏷️</span>
              Rol
            </div>
          </th>
          <th class="px-6 py-4 text-right text-xs font-bold text-purple-900 uppercase tracking-wider">
            <div class="flex items-center justify-end gap-2">
              <span>⚡</span>
              Acciones
            </div>
          </th>
        </tr>
      </thead>
      <tbody class="bg-white divide-y divide-gray-200">
        <% @users.each do |user| %>
          <tr class="hover:bg-gradient-to-r hover:from-purple-50/50 hover:to-pink-50/50 transition">
            <td class="px-6 py-4 whitespace-nowrap">
              <div class="flex items-center gap-3">
                <span class="w-10 h-10 bg-gradient-to-br from-purple-800 to-rose-700 rounded-full flex items-center justify-center text-white text-sm font-bold shadow">
                  <%= user.email[0].upcase %>
                </span>
                <span class="font-medium text-gray-900"><%= user.email %></span>
              </div>
            </td>
            <td class="px-6 py-4 whitespace-nowrap">
              <span class="text-gray-700 font-medium"><%= user.name %></span>
            </td>
            <td class="px-6 py-4 whitespace-nowrap">
              <span class="px-3 py-1.5 text-xs font-bold rounded-lg <%= user.admin? ? 'bg-gradient-to-r from-amber-100 to-amber-200 text-amber-900 border border-amber-400' : 'bg-gradient-to-r from-gray-100 to-gray-200 text-gray-800 border border-gray-300' %>">
                <% if user.admin? %>
                  ⭐ <%= user.role.upcase %>
                <% else %>
                  👤 <%= user.role.upcase %>
                <% end %>
              </span>
            </td>
            <td class="px-6 py-4 whitespace-nowrap text-right text-sm space-x-2">
              <%= link_to edit_admin_user_path(user), class: "inline-flex items-center gap-1 px-3 py-1.5 bg-purple-50 text-purple-800 rounded-lg font-medium hover:bg-purple-100 transition border border-purple-300" do %>
                ✏️ Editar
              <% end %>
              <%= button_to admin_user_path(user), method: :delete, data: { confirm: "¿Eliminar usuario?" }, class: "inline-flex items-center gap-1 px-3 py-1.5 bg-rose-50 text-rose-800 rounded-lg font-medium hover:bg-rose-100 transition border border-rose-300" do %>
                🗑️ Eliminar
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
