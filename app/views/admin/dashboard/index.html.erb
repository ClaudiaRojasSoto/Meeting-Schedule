<div class="mb-8">
  <div class="flex items-center gap-3 mb-2">
    <span class="text-5xl">⚙️</span>
    <h1 class="text-4xl font-bold bg-gradient-to-r from-purple-800 to-rose-700 bg-clip-text text-transparent">
      Panel de Administración
    </h1>
  </div>
  <p class="text-gray-600 text-lg">Gestiona usuarios, reuniones y configuración del sistema</p>
</div>

<div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
  <div class="group bg-white rounded-2xl shadow-lg hover:shadow-2xl transition-all duration-300 overflow-hidden border border-gray-100">
    <div class="bg-gradient-to-br from-purple-800 to-purple-700 px-6 py-8">
      <div class="flex items-center justify-between mb-4">
        <span class="text-5xl">👥</span>
        <span class="px-4 py-2 bg-white/20 backdrop-blur-sm rounded-xl text-white font-semibold">
          Usuarios
        </span>
      </div>
      <h2 class="text-white text-xl font-semibold mb-2">Usuarios Registrados</h2>
      <p class="text-7xl font-bold text-white mb-4"><%= @users_count %></p>
      <%= link_to admin_users_path, class: "inline-flex items-center gap-2 px-5 py-2.5 bg-white text-purple-800 rounded-xl font-semibold hover:shadow-lg transition transform hover:scale-105" do %>
        Administrar Usuarios
        <span class="text-lg">→</span>
      <% end %>
    </div>
  </div>

  <div class="group bg-white rounded-2xl shadow-lg hover:shadow-2xl transition-all duration-300 overflow-hidden border border-gray-100">
    <div class="bg-gradient-to-br from-amber-600 to-amber-700 px-6 py-8">
      <div class="flex items-center justify-between mb-4">
        <span class="text-5xl">📅</span>
        <span class="px-4 py-2 bg-white/20 backdrop-blur-sm rounded-xl text-white font-semibold">
          Reuniones
        </span>
      </div>
      <h2 class="text-white text-xl font-semibold mb-2">Reuniones Programadas</h2>
      <p class="text-7xl font-bold text-white mb-4"><%= @meetings_count %></p>
      <%= link_to meetings_path, class: "inline-flex items-center gap-2 px-5 py-2.5 bg-white text-amber-800 rounded-xl font-semibold hover:shadow-lg transition transform hover:scale-105" do %>
        Ver Reuniones
        <span class="text-lg">→</span>
      <% end %>
    </div>
  </div>
</div>

<div class="bg-white shadow-xl rounded-2xl overflow-hidden border border-gray-100">
  <div class="bg-gradient-to-r from-purple-900 to-rose-800 px-8 py-6">
    <div class="flex items-center gap-3">
      <span class="text-4xl">📋</span>
      <h2 class="text-3xl font-bold text-white">Próximas Reuniones</h2>
    </div>
  </div>
  
  <div class="p-8">
    <% if @upcoming_meetings.empty? %>
      <div class="text-center py-12">
        <div class="text-6xl mb-4">📅</div>
        <p class="text-xl text-gray-500">No hay reuniones próximas programadas</p>
      </div>
    <% else %>
      <div class="space-y-3">
        <% @upcoming_meetings.each do |meeting| %>
          <div class="group flex items-center gap-4 p-4 bg-gradient-to-r from-purple-50 to-pink-50 rounded-xl border border-purple-200 hover:border-purple-400 hover:shadow-lg transition">
            <div class="flex-shrink-0 w-14 h-14 bg-gradient-to-br from-purple-800 to-rose-700 rounded-xl flex flex-col items-center justify-center text-white shadow-lg">
              <span class="text-xs font-medium uppercase"><%= meeting.date.strftime("%b") %></span>
              <span class="text-xl font-bold"><%= meeting.date.strftime("%d") %></span>
            </div>
            <div class="flex-1">
              <%= link_to meeting.title, meeting_path(meeting), class: "font-bold text-lg text-gray-900 hover:text-purple-700 transition" %>
              <p class="text-sm text-gray-500"><%= meeting.date.strftime("%A, %d de %B de %Y") %></p>
            </div>
            <%= link_to meeting_path(meeting), class: "px-4 py-2 bg-purple-100 text-purple-800 rounded-lg font-medium hover:bg-purple-200 transition border border-purple-300" do %>
              Ver →
            <% end %>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
