<div class="max-w-5xl mx-auto">
  <div class="mb-8">
    <div class="flex items-center gap-3 mb-2">
      <span class="text-4xl">👥</span>
      <h2 class="text-4xl font-bold bg-gradient-to-r from-purple-800 to-rose-700 bg-clip-text text-transparent">
        Asignar Participantes
      </h2>
    </div>
    <p class="text-gray-600 text-lg"><%= @meeting.title %> - <%= @meeting.date.strftime("%d/%m/%Y") %></p>
  </div>

  <div class="bg-white rounded-2xl shadow-xl p-8 border border-purple-100">
    <%= form_with url: bulk_assign_admin_meeting_path(@meeting), method: :post, local: true do |f| %>
      <div class="space-y-6">
        <% if @unassigned_items.empty? %>
          <div class="text-center py-12">
            <div class="text-6xl mb-4">✅</div>
            <h3 class="text-2xl font-semibold text-gray-800 mb-2">Todos los items tienen asignación</h3>
            <p class="text-gray-500 mb-6">No hay participantes pendientes de asignar</p>
            <%= link_to "Ver reunión", meeting_path(@meeting), class: "inline-flex items-center px-6 py-3 bg-gradient-to-r from-purple-800 to-purple-700 text-white rounded-xl font-medium hover:shadow-lg transition" %>
          </div>
        <% else %>
          <% @meeting.schedule_items.select(&:persisted?).each_with_index do |item, index| %>
            <div class="bg-gradient-to-r from-purple-50 to-rose-50 rounded-xl p-6 border border-purple-200">
              <div class="flex items-start gap-4 mb-4">
                <div class="flex-shrink-0 w-12 h-12 bg-gradient-to-br from-purple-800 to-rose-700 rounded-xl flex items-center justify-center text-white font-bold text-lg shadow-lg">
                  <%= index + 1 %>
                </div>
                <div class="flex-1">
                  <h3 class="text-xl font-bold text-gray-900 mb-1"><%= item.role %></h3>
                  <div class="flex items-center gap-3 text-sm text-gray-600 mb-2">
                    <% if item.start_time.present? %>
                      <span class="flex items-center gap-1">
                        <span>🕐</span>
                        <%= item.start_time.strftime("%H:%M") %>
                      </span>
                    <% end %>
                    <span class="flex items-center gap-1">
                      <span>⏱️</span>
                      <%= item.duration_display %>
                    </span>
                  </div>
                  <% if item.notes.present? %>
                    <p class="text-sm text-gray-600 mt-2 pl-3 border-l-2 border-purple-300"><%= item.notes %></p>
                  <% end %>
                </div>
              </div>
              
              <div>
                <label class="block text-sm font-bold text-gray-700 mb-2">
                  <span class="flex items-center gap-2">
                    <span>👤</span>
                    Asignar a:
                  </span>
                </label>
                <div class="relative">
                  <%= select_tag "assignments[#{item.id}]", 
                      options_for_select(
                        [["Sin asignar", ""]] + @users.map { |u| [u.name || u.email, u.name || u.email] },
                        item.speaker
                      ),
                      class: "w-full px-4 py-3 border-2 border-purple-300 rounded-xl focus:ring-4 focus:ring-purple-100 focus:border-purple-600 transition appearance-none bg-white text-lg" %>
                  <span class="absolute right-4 top-1/2 -translate-y-1/2 text-xl pointer-events-none">📋</span>
                </div>
              </div>
            </div>
          <% end %>

          <div class="flex gap-3 pt-4">
            <%= f.submit "Guardar Asignaciones", class: "flex-1 md:flex-none px-8 py-4 bg-gradient-to-r from-purple-800 to-purple-700 text-white rounded-xl font-bold text-lg hover:shadow-2xl transition transform hover:scale-105 cursor-pointer" %>
            <%= link_to "Cancelar", meeting_path(@meeting), class: "flex-1 md:flex-none px-8 py-4 bg-white border-2 border-gray-300 text-gray-700 rounded-xl font-bold text-lg hover:border-gray-400 hover:shadow-md transition text-center" %>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>
</div>

