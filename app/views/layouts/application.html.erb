<!DOCTYPE html>
<html>
  <head>
    <title>Meeting Schedule</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/@heroicons/react@2.0.18/24/outline/index.js" type="module"></script>
    <style>
      body { font-family: 'Inter', sans-serif; }
    </style>
  </head>

  <body class="bg-gradient-to-br from-purple-50 via-pink-50 to-rose-50 min-h-screen">
    <nav class="bg-gradient-to-r from-purple-900 to-purple-800 shadow-lg sticky top-0 z-50">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-16">
          <div class="flex items-center space-x-8">
            <h1 class="text-2xl font-bold text-white">
              <%= link_to "📅 Meeting Schedule", root_path, class: "hover:text-amber-200 transition" %>
            </h1>
            <% if user_signed_in? %>
              <div class="hidden md:flex space-x-1">
                <%= link_to meetings_path, class: "px-4 py-2 rounded-lg text-sm font-medium text-purple-100 hover:bg-purple-800 hover:text-white transition" do %>
                  📋 Reuniones
                <% end %>
                <% if current_user.admin? %>
                  <%= link_to admin_dashboard_path, class: "px-4 py-2 rounded-lg text-sm font-medium text-purple-100 hover:bg-purple-800 hover:text-white transition" do %>
                    ⚙️ Admin
                  <% end %>
                <% end %>
              </div>
            <% end %>
          </div>
          <div class="flex items-center space-x-3">
            <% if user_signed_in? %>
              <div class="hidden sm:flex items-center space-x-2 px-3 py-1.5 bg-purple-800/50 rounded-lg">
                <span class="w-8 h-8 bg-gradient-to-br from-amber-500 to-amber-600 rounded-full flex items-center justify-center text-white text-sm font-bold shadow-lg">
                  <%= current_user.email[0].upcase %>
                </span>
                <span class="text-sm font-medium text-white"><%= current_user.email.split('@').first %></span>
              </div>
              <%= button_to destroy_user_session_path, method: :delete, class: "px-4 py-2 bg-rose-700 hover:bg-rose-800 text-white rounded-lg font-medium text-sm transition shadow-sm hover:shadow" do %>
                Salir
              <% end %>
            <% else %>
              <%= link_to "Iniciar Sesión", new_user_session_path, class: "px-6 py-2.5 bg-gradient-to-r from-amber-500 to-amber-600 text-white rounded-lg font-medium text-sm hover:shadow-lg transition transform hover:scale-105" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <% if notice %>
        <div class="mb-6 p-4 bg-emerald-50 border-l-4 border-emerald-600 text-emerald-800 rounded-lg shadow-sm animate-fade-in">
          <div class="flex items-center">
            <span class="text-2xl mr-3">✅</span>
            <span class="font-medium"><%= notice %></span>
          </div>
        </div>
      <% end %>
      <% if alert %>
        <div class="mb-6 p-4 bg-rose-50 border-l-4 border-rose-600 text-rose-800 rounded-lg shadow-sm animate-fade-in">
          <div class="flex items-center">
            <span class="text-2xl mr-3">⚠️</span>
            <span class="font-medium"><%= alert %></span>
          </div>
        </div>
      <% end %>

      <%= yield %>
    </main>

    <style>
      @keyframes fade-in {
        from { opacity: 0; transform: translateY(-10px); }
        to { opacity: 1; transform: translateY(0); }
      }
      .animate-fade-in { animation: fade-in 0.3s ease-out; }
    </style>
  </body>
</html>
